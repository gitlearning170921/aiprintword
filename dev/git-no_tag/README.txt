git-no_tag — aiprintword 日常 Git（不打 tag，不进 Docker 打包链）

  commit_push.bat "说明"    add + commit + push
  push_only.bat             仅 push

aiprintword 不参与 aiword/aicheckword 的 v 版本 tag 发版。
需要单独发版时可手动 git tag（一般不必）。
