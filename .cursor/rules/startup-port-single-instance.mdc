---
description: 启动服务前做端口单实例自检，避免 5050 被占用导致页面打不开/HTTP 超时
globs: app.py
alwaysApply: true
---

# 启动自检：端口 5050 单实例

## 背景

在 Windows 上经常会不小心启动了多个 `python app.py`（或旧窗口未关），导致 **同一端口 5050 被多个进程监听**。
此时“看似服务启动正常”，但浏览器可能连到旧进程，表现为 **页面打不开、请求超时、或行为与代码不一致**。

## 规则

- `app.py` 在 `__main__` 启动 `app.run(..., port=5050)` 前必须做 **端口占用自检**：
  - 若端口已被占用：在控制台打印 **明确提示**“已有其它 app.py 在跑，先关掉它”，并 **退出进程**（非 0 退出码）。
  - 提示里给出 Windows 的排查/关闭命令（`netstat -ano | findstr :5050` / `taskkill /PID <PID> /F`），Linux/macOS 给出 `lsof`/`kill`。
- 前端/后端排错文案里遇到“页面打不开/请求超时/405/404 像旧服务”的情况，优先提示用户：
  - **只保留一个** `python app.py`
  - **重启服务 + Ctrl+F5** 强刷

